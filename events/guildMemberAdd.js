module.exports = {

	name: 'guildMemberAdd',
	once: false,
	execute(member) {

		// get guild.invites()

		// loop through guild invites
			// match in invite_codes?
		  		// no? add it

			// Check if looped invite has more uses than tracked uses in db

			// if it was used
				// insert users into users table
				// insert invite into invites table
				// update invite_codes entry usage

	},

};