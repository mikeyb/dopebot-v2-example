const BaseEmbed = require('./_base');

const InfoEmbeds = {};

InfoEmbeds.ExplainDope = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('What is a "DOPE NFT"❓')
        .setDescription('Limited Edition `DOPE NFT` - 8,000 NFTs')
        .addField('DOPE NFTs', '' +
            '`Total DOPE NFTs`' + "\n" +
            'A limited-edition of `8,000` **DOPE NFTs** were created in __September 2021__.' + "\n\n" +

            '`DOPE NFT Contents' + "\n" +
            'Each `DOPE NFT` contains eight pieces of wearable `Gear` with randomized attributes, rarity scores and one vehicle.' + "\n" +
            'Each `DOPE NFT` allows you to `Claim $PAPER` as well as `Claim Gear` directly to your wallet or `Initiate a Hustler` with the gear.' + "\n\n" +

            '`Additional Info`' + "\n" +
            'A `DOPE NFT` can only be used **one time** for `$PAPER` & `Gear`.'  + "\n\n" +

            '`Fully Claimed DOPE NFT`' + "\n" +
            'Once fully claimed the DOPE NFT still holds value by providing one **DAO Governance Vote** on [DopeWars Proposals](https://dope-wars.notion.site/626df3ff9e4d47da98ea23abc4b6e7a7).' + "\n\n" +
            'The `DOPE NFT` will serve as a **Dope Pass** for future additions to the game and metaverse, including eligibility for future airdops, staking and features.',
            false
        );

    return Embed;

};


InfoEmbeds.ExplainClaimed = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainOpened = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainHustler = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainOGs = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainGear = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainTurf = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainPaper = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

InfoEmbeds.ExplainStreetCred = async function () {

    const Embed = BaseEmbed;

    Embed
        .setTitle('')
        .setDescription('')

    return Embed;

};

module.exports = InfoEmbeds;